---
export const prerender = false;

/**
 * Beyond The Books sub-collection page — /suture/beyond-the-books/[sub]
 */
import PageLayout from '../../../layouts/PageLayout.astro';
import EditorialCard from '../../../components/EditorialCard.astro';
import { getArticles, BOOKS_ROUTE_MAP, COLLECTION_NAMES } from '../../../lib/cms';

const { sub } = Astro.params as { sub: string };
const collectionId = BOOKS_ROUTE_MAP[sub];
if (!collectionId) return Astro.redirect('/suture/beyond-the-books');

const sectionName = COLLECTION_NAMES[sub] ?? sub;
const articles = await getArticles(collectionId, 24);
---

<PageLayout title={sectionName} description={`${sectionName} — Beyond The Books, The Suture.`}>
  <div style="padding-top: 64px;">
    <div class="section-landing__banner">
      <div class="container">
        <p class="eyebrow" data-reveal>Beyond The Books</p>
        <h1 class="section-landing__title" data-reveal data-delay="100">{sectionName}</h1>
      </div>
    </div>

    <div class="container section-py">
      {articles.length > 0 ? (
        <div class="section-landing__grid">
          {articles.map((article, i) => (
            <EditorialCard
              article={article}
              href={`/suture/beyond-the-books/${Astro.params.sub}/${article.slug}`}
              sectionLabel={sectionName}
              delay={Math.min(i * 80, 400)}
            />
          ))}
        </div>
      ) : (
        <p class="empty-state">No articles yet. Check back soon.</p>
      )}
    </div>
  </div>
</PageLayout>

<style>
  .section-landing__banner {
    background: var(--color-surface);
    border-bottom: 1px solid var(--color-border);
    padding-block: var(--space-12) var(--space-10);
  }
  .section-landing__title {
    font-family: var(--font-serif);
    font-size: var(--fs-3xl);
    font-weight: 700;
    letter-spacing: var(--ls-tight);
  }
  .section-landing__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--space-6);
  }
  .empty-state {
    text-align: center;
    padding-block: var(--space-20);
    color: var(--color-text-tertiary);
  }
</style>
